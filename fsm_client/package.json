{
  "name": "fsm_client",
  "version": "0.1.0",
  "description": "fsm client which connects to the site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redis": "^0.12.1",
    "request": "^2.42.0"
  }
}
